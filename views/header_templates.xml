<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!--    Template customized for header-->
    <template id="ecom_header" inherit_id="website.layout" name="Ecom Header" active="True">
        <xpath expr="//header" position="replace">
            <header t-if="not no_header">
                <!-- Navigation -->
                <nav class="navbar navbar-expand-xl navbar-dark fixed-top hotcodes-navbar">
                    <div class="container">
                        <!-- Logo - Left Column -->



                        <a class="navbar-brand hotcodes-logo" href="/">
                            <img src="/theme_hotcodes/static/src/img/logo.svg" class="img-fluid" aria-label="Logo of MyCompany" title="MyCompany" role="img"/>
                        </a>

                        <!-- Mobile Toggle Button -->
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                            <span class="navbar-toggler-icon"></span>
                        </button>

                        <!-- Collapsible Content -->
                        <div class="collapse navbar-collapse" id="navbarNav">
                            <!-- Navigation Menu - Center Column -->
                            <t t-call="website.navbar_nav">
                                <t t-set="_no_autohide_menu_mobile" t-valuef="True" />
                                <t t-set="_nav_class" t-valuef="navbar-nav mx-auto" />
                                <t t-foreach="website.menu_id.child_id" t-as="submenu">
                                    <t t-call="website.submenu">
                                        <t t-set="item_class" t-valuef="nav-item" />
                                        <t t-set="link_class" t-valuef="nav-link" />
                                    </t>
                                </t>
                            </t>

                            <!-- Action Button - Right Column -->
                            <div class="navbar-actions ms-auto">
                                <a href="/demande-demo" class="btn btn-primary" style="color: white !important;">
                                    Demander une Démo
                                    <iconify-icon icon="solar:play-circle-bold-duotone" class="ms-1" style="font-size: 1.1em; vertical-align: middle;"></iconify-icon>
                                </a>
                            </div>
                        </div>
                    </div>
                </nav>
            </header>

        </xpath>
    </template>
</odoo>